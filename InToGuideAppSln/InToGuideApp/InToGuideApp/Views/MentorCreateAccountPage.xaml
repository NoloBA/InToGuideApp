<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="InToGuideApp.Views.MentorCreateAccountPage"
             Title="{Binding Title}">
    <ScrollView>
        <StackLayout BackgroundColor="WhiteSmoke">
            
            <Line Stroke="#F79726"  X2="300" StrokeThickness="3" HorizontalOptions="CenterAndExpand" Margin="0,10,0,0"/>
            <Label Text="Create Mentor Account" FontSize="50" TextColor="Black"  HorizontalTextAlignment="Center"></Label>
            <Line Stroke="#F79726"  X2="300" StrokeThickness="3" HorizontalOptions="CenterAndExpand" Margin="0,0,0,0"/>

            <Entry x:Name="accountTypeEntry" Text="{Binding AccountType}" Placeholder="Account Type" Keyboard="Text"></Entry>
            
            <Entry x:Name="firstNameEntry" Text="{Binding FirstName}" Placeholder="First Name" Keyboard="Text"></Entry>


            <Entry x:Name="lastNameEntry" Text="{Binding LastName}" Placeholder="Last Name" Keyboard="Text"></Entry>


            <Entry x:Name="qualificationEntry" Text="{Binding Qualification}" Placeholder="Qualification" Keyboard="Text"></Entry>


            <Entry x:Name="institutionEntry" Text="{Binding Institution}" Placeholder="Institution" Keyboard="Text"></Entry>


            <Entry x:Name="locationEntry" Text="{Binding Location}" Placeholder="Location" Keyboard="Text"></Entry>

            <Entry x:Name="phoneNumberEntry" Text="{Binding PhoneNumber}" Placeholder="Phone Number" Keyboard="Telephone">
                <!--<Entry.Behaviors>
                    <local:PhoneNumberValidationBehaviour></local:PhoneNumberValidationBehaviour>
                </Entry.Behaviors>-->
            </Entry>

            <Entry x:Name="idNumberEntry" Text="{Binding IdNumber}" Placeholder="ID Number" Keyboard="Text"></Entry>


            <Entry x:Name="hobbiesEntry" Text="{Binding Hobbies}" Placeholder="Hobbies" Keyboard="Text"></Entry>

            <Entry x:Name="emailAddressEntry" Text="{Binding EmailAddress}" Placeholder="Email Address" Keyboard="Email">
                <!--<Entry.Behaviors>
                    <local:EmailValidationBehaviour></local:EmailValidationBehaviour>
                </Entry.Behaviors>-->
            </Entry>

            <Entry x:Name="passwordEntry" Text="{Binding Password}" Placeholder="Password" Keyboard="Text"></Entry>
            

            <!--<Label Text="Company you working for" HorizontalOptions="Start"/>
            <Frame  BorderColor="#f79726" CornerRadius="50" Margin="5,0,5,0" Padding="-7">
                <Entry></Entry>
            </Frame>

            <Label Text="Position in the company/ Rank" HorizontalOptions="Start"/>
            <Frame  BorderColor="#f79726" CornerRadius="50" Margin="5,0,5,0" Padding="-7">
                <Entry></Entry>
            </Frame-->

            <Button x:Name="CreateMentorAccount" Text="Create Account" CornerRadius="50" Margin="30,0,30,0" BorderColor="#f79726" BackgroundColor="White"
                    Command="{Binding CreateMentorAccountCommand}"/>

            <Label Text="{Binding Message}" TextColor="Green" />

        </StackLayout>
    </ScrollView>
   
</ContentPage>